import React from "react";

export const distancingSidebar = () => {
    return(
        <div>distancing</div>
    );
};

export const distancing = () => {
    return(
        <div>distancing</div>
    );
};